# 鎖定版本(由 MyTools Launcher 1.1.0 嚴格參數驗證可解,2026-05-26)
# 目標環境: Python 3.14 / cp314 / win_amd64,僅使用 wheel(--only-binary :all:)
# 修改前請先用 launcher 同條件 pip --dry-run 驗證。

# ── 直接相依 ──
ezdxf==1.4.4
matplotlib==3.10.9
pypdf==6.12.1
pywin32==311; sys_platform == "win32"

# ── 間接相依(鎖版,確保任何使用者安裝的結果一致) ──
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
numpy==2.4.6
packaging==26.2
pillow==12.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.15.0
